# Optional. Without a key undertone runs a built-in keyword classifier.
# Get one at https://console.typesafe.ai/keys. Read on the server only.
TYPESAFE_API_KEY=

# Pin a model version so readings don't shift under you.
JEV_MODEL=jev-latest
